---
title: August 16th
description: 今天是预习很多的知识的一天
type: knowledge
pubDate: Aug 16 2026
updatedDate: Aug 16 2026
---
整理一下今天稍微易混的知识(细节都在我总结的pdf里面，这里都只是知识点概括）：

一：sc2001

1：插入排序，快速排序，merge排序；前缀和排序

2:0based 和1 based的完全二叉树下标

3：（最小）堆排序；建堆；最小值；出队入队，怎么用堆排序（先最大堆，再依次取出元素）

4：图，子图和一些基本的概念。什么是连通分量，连通搜索；图的遍历中DFS和BFS的时间复杂度都是一样的。因为dfs也会控制visited来控制多少；边的分类（前向边和后向边）；拓扑排序

5：最短路径（最优子结构，三角不等式）；负权边和负权环；bellman_ford,dijkstra(基于优先队列；不能检测负权边和环）

6：最小生成树（边权）；割与割性质；Prime算法；并查集（压缩路径：压扁成环）（按秩合并：维护根节点的rank）；kruskal算法（先排序；合并；并且用并查集检查是否成环）

二：sc1006

1:内存是代码段和数据段。ARM指令集架构，操作码和操作数

2：16个寄存器的作用（12通用寄存器；栈指针；链接寄存器；下一条；保存标志位；pc的值（+8）和每次移动距离

3：LDR的执行方法（取指--译码--取数--执行）

4：基本执行周期，冯诺依曼瓶颈--指令放入内存；哈佛并行执行

5：寻址方式（汇编语言）--ARM支持的寻址方式一览

6：12位立即数如何编码32位值（旋转二进制位置）

8：系统栈（满递增，满递减，空递增，空递减）（SP；用来保存等会儿还要用到的信息）

9：PC（系统现在应该去哪里执行代码）：绝对跳转/相对跳转，相对寻址（偏移量？）（pipline让pc一直领先8）

10：
